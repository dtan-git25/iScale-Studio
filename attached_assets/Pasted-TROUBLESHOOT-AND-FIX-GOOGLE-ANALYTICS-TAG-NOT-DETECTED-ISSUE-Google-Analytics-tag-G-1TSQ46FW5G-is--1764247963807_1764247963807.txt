TROUBLESHOOT AND FIX GOOGLE ANALYTICS TAG NOT DETECTED

ISSUE: Google Analytics tag G-1TSQ46FW5G is not being detected on the live website.

═══════════════════════════════════════════════════════════
DIAGNOSTIC STEPS
═══════════════════════════════════════════════════════════

## Step 1: Verify Code Was Actually Added

Check if the Google Analytics code exists in the HTML file:

1. Open the index.html file (in /public, /src, or root)
2. Search for: "G-1TSQ46FW5G"
3. If NOT found → Code was never added (go to Step 2)
4. If found → Code exists but not deployed (go to Step 3)

## Step 2: Add Google Analytics Code (If Missing)

If the code is NOT in the index.html file, add it now:

**Location:** Find the <head> section in index.html

**Add this code inside <head>, near the top:**
```html
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-1TSQ46FW5G"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  
  gtag('config', 'G-1TSQ46FW5G');
</script>
```

**CRITICAL:** Place this AFTER the opening <head> tag but BEFORE the closing </head> tag.

## Step 3: Verify Correct File Location

The code MUST be in the correct index.html file:

**Common file locations:**
- `/index.html` (root level - if static site)
- `/public/index.html` (if using Vite, React, or similar)
- `/src/index.html` (some build systems)

**How to verify correct file:**
1. Check your build configuration
2. The file that gets served at https://iscalestudio.com should contain the GA code
3. If using a build system (Vite, Webpack, etc.), the code should be in the SOURCE index.html, not the built output

**For Vite/React projects:**
- Source file: `/index.html` (root) or `/public/index.html`
- Build output: `/dist/index.html` (this is generated from source)
- Add code to SOURCE file, NOT the build output

## Step 4: Rebuild the Project

After adding the code to the correct file:

1. Run the build command:
npm run build
   or
yarn build

2. Verify the code appears in the build output:
   - Check `/dist/index.html` or `/build/index.html`
   - Search for "G-1TSQ46FW5G" in the built file
   - If NOT found in build output → Build process isn't copying the file correctly

## Step 5: Deploy to Production

After successful build:

1. Deploy the updated files to production (Replit will auto-deploy if configured)
2. Wait 2-3 minutes for deployment to complete
3. Clear your browser cache (Ctrl+Shift+R or Cmd+Shift+R)

## Step 6: Verify Deployment

Check if the code is now live:

1. Visit: https://iscalestudio.com
2. Right-click → "View Page Source" (or Ctrl+U)
3. Search for: "G-1TSQ46FW5G"
4. If found → Code is deployed ✅
5. If NOT found → Continue to Step 7

═══════════════════════════════════════════════════════════
ALTERNATIVE APPROACH (If Above Doesn't Work)
═══════════════════════════════════════════════════════════

## Option A: Add to HTML Template

If using a React/Vue/framework that has a template file:

**Look for:**
- `index.html` in the root directory
- HTML template in `/public` folder
- `template.html` file

**Ensure you're editing the TEMPLATE, not the generated output.**

## Option B: Add Via Component (React/Vue)

If the above doesn't work, add via your main App component:

**For React (in App.jsx or App.js):**
```javascript
import { useEffect } from 'react';

function App() {
  useEffect(() => {
    // Load gtag script
    const script1 = document.createElement('script');
    script1.async = true;
    script1.src = 'https://www.googletagmanager.com/gtag/js?id=G-1TSQ46FW5G';
    document.head.appendChild(script1);

    // Initialize gtag
    const script2 = document.createElement('script');
    script2.innerHTML = `
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-1TSQ46FW5G');
    `;
    document.head.appendChild(script2);
  }, []);

  return (
    // Your existing App component JSX
  );
}
```

## Option C: Add to _document.js (Next.js)

If using Next.js:

**Create/edit `/pages/_document.js`:**
```javascript
import { Html, Head, Main, NextScript } from 'next/document'

export default function Document() {
  return (
    <Html>
      <Head>
        <script
          async
          src="https://www.googletagmanager.com/gtag/js?id=G-1TSQ46FW5G"
        />
        <script
          dangerouslySetInnerHTML={{
            __html: `
              window.dataLayer = window.dataLayer || [];
              function gtag(){dataLayer.push(arguments);}
              gtag('js', new Date());
              gtag('config', 'G-1TSQ46FW5G');
            `,
          }}
        />
      </Head>
      <body>
        <Main />
        <NextScript />
      </body>
    </Html>
  )
}
```

═══════════════════════════════════════════════════════════
COMMON ISSUES & SOLUTIONS
═══════════════════════════════════════════════════════════

**Issue 1: Code in wrong file**
- Solution: Find the actual index.html that gets served
- Check build output directory

**Issue 2: Code added but not rebuilt**
- Solution: Run build command and redeploy

**Issue 3: Build process removes the code**
- Solution: Check if your build tool is minifying/removing scripts
- Add to a template file that doesn't get processed

**Issue 4: Cached version showing**
- Solution: Hard refresh browser (Ctrl+Shift+R)
- Try in incognito/private window

**Issue 5: SPA routing issue**
- Solution: Ensure code is in the main index.html, not in routed components

═══════════════════════════════════════════════════════════
VERIFICATION CHECKLIST
═══════════════════════════════════════════════════════════

Complete each step and confirm:

- [ ] Code added to correct index.html file
- [ ] Code is inside <head> section
- [ ] Both <script> tags are present
- [ ] Tracking ID is G-1TSQ46FW5G (not modified)
- [ ] File saved successfully
- [ ] Project rebuilt (npm run build)
- [ ] Code appears in /dist/index.html or /build/index.html
- [ ] Project deployed to production
- [ ] Wait 2-3 minutes after deployment
- [ ] Hard refresh browser cache
- [ ] View page source shows G-1TSQ46FW5G
- [ ] Browser DevTools Network tab shows gtag requests

═══════════════════════════════════════════════════════════
IMMEDIATE ACTION REQUIRED
═══════════════════════════════════════════════════════════

1. **First, tell me:** Did you add the Google Analytics code to the index.html file?
   - YES → Which file path? (e.g., /public/index.html)
   - NO → Add it now to the correct file

2. **After adding, run:**
   - Build command (npm run build or equivalent)
   - Deploy to production

3. **After deployment:**
   - Wait 2-3 minutes
   - Clear browser cache
   - Check page source for G-1TSQ46FW5G

4. **Report back:**
   - Can you see "G-1TSQ46FW5G" in the page source?
   - What is the exact file path where you added the code?

Please provide this information so I can help troubleshoot further.

═══════════════════════════════════════════════════════════
EXPECTED OUTCOME
═══════════════════════════════════════════════════════════

After successful implementation:
- Page source will show the Google Analytics code
- Browser DevTools Network tab will show requests to:
  - www.googletagmanager.com/gtag/js?id=G-1TSQ46FW5G
  - www.google-analytics.com/g/collect
- Google Analytics will detect the tag within 24 hours
- Real-time reports will show active users immediately

If still not working after following all steps, provide:
1. The exact file path where you added the code
2. Screenshot of the <head> section
3. Screenshot of page source showing the <head> area